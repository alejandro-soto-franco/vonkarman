# =============================================================================
# vonkarman .gitignore — high-precision numerical simulation
# =============================================================================

# ---------------------------------------------------------------------------
# Build artifacts
# ---------------------------------------------------------------------------
/target/
**/*.d
**/*.o
**/*.so
**/*.dylib
**/*.a
**/*.lib
**/*.dll
**/*.exe

# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
docs/

# ---------------------------------------------------------------------------
# Simulation output (large, machine-specific)
# ---------------------------------------------------------------------------
output/
runs/
sweeps/
*.h5
*.hdf5
*.parquet
*.arrow
*.checkpoint
*.checkpoint.tmp
*.bin
manifest.json
manifest.json.tmp
run.log

# ---------------------------------------------------------------------------
# CUDA / GPU build artifacts
# ---------------------------------------------------------------------------
*.ptx
# The runtime kernel PTX the GPU backend embeds is a tracked source artifact.
!vonkarman-compute/src/ptx/*.ptx
*.cubin
*.fatbin
# cuda-oxide intermediate LLVM IR dumps (regenerated by `cargo oxide build`)
*.ll
*.gpu
*.i
*.ii
*.cu.o
**/*.cu.deps

# ---------------------------------------------------------------------------
# VkFFT build artifacts
# ---------------------------------------------------------------------------
vkfft_build/
*.spv

# ---------------------------------------------------------------------------
# FFTW wisdom files (machine-specific, regenerated at runtime)
# ---------------------------------------------------------------------------
*.wisdom
fftw_wisdom*

# ---------------------------------------------------------------------------
# High-precision intermediate data
# ---------------------------------------------------------------------------
*.mpfr
*.qd
*.dd
*.highprec

# ---------------------------------------------------------------------------
# IDE / editor
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# ---------------------------------------------------------------------------
# Profiling / benchmarks
# ---------------------------------------------------------------------------
flamegraph.svg
perf.data
perf.data.old
*.prof
criterion/
cachegrind.out.*
callgrind.out.*

# Large HIT3D benchmark data (purged from history; local copies in
# ~/vonkarman-benchmark-data-backup). Regeneratable; never commit.
benchmarks/hit3d/u.*
benchmarks/hit3d/v.*
benchmarks/hit3d/w.*
benchmarks/hit3d/tg_*

# ---------------------------------------------------------------------------
# Environment / secrets
# ---------------------------------------------------------------------------
.env
.env.*
*.secret
*.key
*.pem

# ---------------------------------------------------------------------------
# OS artifacts
# ---------------------------------------------------------------------------
Thumbs.db
Desktop.ini
._*

# ---------------------------------------------------------------------------
# Temporary files
# ---------------------------------------------------------------------------
*.tmp
*.temp
*.bak
*.orig
*.rej
tmp/
temp/

# ---------------------------------------------------------------------------
# Coverage / test artifacts
# ---------------------------------------------------------------------------
*.gcda
*.gcno
*.gcov
tarpaulin-report.html
lcov.info
coverage/

# ---------------------------------------------------------------------------
# Package manager
# ---------------------------------------------------------------------------
Cargo.lock
# Cargo.lock is excluded for libraries (workspace of libs).
# If vonkarman-bin becomes the primary consumer, move this to .gitignore exceptions.

# ---------------------------------------------------------------------------
# Core dumps
# ---------------------------------------------------------------------------
core
core.*
vgcore.*

# AI tooling

# Local scratch symlink, never in the published tree
output
